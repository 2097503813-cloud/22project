# ---------------- Python ----------------
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# 本地数据库配置（含账号密码，不入库；克隆后按 项目交接文档.md §1 自己建一份 db.env）
db.env
.env
.env.local

# ---------------- Node / 前端 ----------------
node_modules/
dist/
.vite/
*.local
npm-debug.log*

# ---------------- 运行期产物（可重新生成，不入库）----------------
testRestfulProject/data/logs/
testRestfulProject/data/figures/
testRestfulProject/data/tmp/
testRestfulProject/data/.tmpcheck/
data/logs/
data/figures/

# 旧版本产物的归档（去掉版本号时由 registry.archive_legacy_versions() 搬进去的）。
# 刻意不入库：它们只是"本地留个底、不删"，不属于项目布局的一部分；
# data/models/ 下的**当前**产物仍然入库，保证克隆下来就有可用的模型。
testRestfulProject/data/archive/

# ---------------- 暂时挪走的 dvadmin 无用代码 ----------------
# 见 项目技术详解.md §8：想恢复就把它移回 frontend/22project/src/
_unused_dvadmin/

# ---------------- 编辑器 / 系统 ----------------
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini

# DSH 工具自身的文件
.deepseek/
testRestfulProject/data/uploads/
testRestfulProject/data/**/*.db

# 一次性/临时脚本不入库；但 **_selftest_*.py 是正式测试，必须入库**。
# ⚠️ 这条例外是踩了坑才补的：`_selftest_predict_path.py` 因为落在 `_*.py` 这条规则里而从未被跟踪，
#    被误删之后 git 里根本没有它、无法恢复，只能按原来的断言重写一份。测试脚本不能再这样丢。
testRestfulProject/_*.py
!testRestfulProject/_selftest_*.py

testRestfulProject/requirements.lock

# 临时审计/扫描产物
_dcscan_tmp/

# ---------------- 离线部署包（不入库！）----------------
# offline_package/ 是用优盘搬去实验室的**成品**，里面是 1.3 GB 的二进制
# （86 个 wheel + 45,304 个 node_modules 文件），github 单文件 100MB 上限顶不住，
# 也不该让仓库变成几百 MB。它属于"交付物"，不属于"源码"。
#
# 想重建这个包，照《离线部署手册》第 1 节的命令重新生成即可 —— 命令都在文档里，
# 所以这里排除它不会造成信息丢失。
offline_package/

# 打包/核对过程中的中间产物
docs/_dl/
*.tmp.whl

# 代码更新包（make-update-package.ps1 的产物，优盘用的，不进仓库）
code-update-*/
