# 轴承故障诊断平台 · 项目交接文档

> 生成时间：2026-09-12 ｜ 覆盖范围：`D:\22project` 全工作区
> 用途：上下文清理后的完整交接。包含架构、接口、数据库、前后端定制点、踩坑记录、遗留问题、常用命令。

---

## 0. 一句话概览

**后端** = 自己写的 Flask 服务（`testRestfulProject/model_service`，端口 **5000**），负责「数据集 → 训练 → 模型产物 → 推理 → 落库 → 出图」全链路；
**前端** = `frontend/22project`（开源框架 **django-vue3-admin / dvadmin 3.2.0**，Vue3 + Vite5 + Element Plus，端口 **8080**），我在这套框架里加了 5 个业务模块；
**数据库** = 本机 **MySQL 9.2**（`model_management` 库，8 张表）。
**前端不依赖 Django**：dvadmin 原本要的后端接口（登录、用户信息、动态菜单、字典…）我在 Flask 里做了一层兼容实现。

```
D:\22project
├─ testRestfulProject\            后端（Flask）+ 原始算法脚本 + 数据库脚本
│  ├─ main.py                     Flask 入口（/todos 示例 + 注册 model_service + dvadmin 兼容层）
│  ├─ model_service\              ★ 后端核心包（我写的）
│  ├─ 1DCNN\                      原始教程脚本：TF/Keras 版本 + 0HP 数据集
│  ├─ cwt_cnn\                    原始教程脚本：PyTorch 版本 + 同一批 0HP（重复副本）
│  ├─ adtk\                       第三方时序异常检测库（整份拷贝进来的源码）
│  ├─ sql\                        schema.sql(T-SQL) / schema_mysql.sql / schema_sqlite.sql(我加的)
│  ├─ data\                       ★ 运行时产物（模型/图/日志/上传的数据集）
│  ├─ db.env                      ★ 数据库连接配置（服务启动自动读取）
│  ├─ createdoc.py                与本平台无关的小工具（Word 批量生成）
│  └─ venv\                       Python 3.14.7 虚拟环境
└─ frontend\22project\            ★ 前端（dvadmin 框架 + 我加的 5 个模块）
   ├─ src\views\platform\         首页/模型管理/数据集管理/数据展示/系统管理
   ├─ src\api\platform\index.ts   平台接口封装
   ├─ src\utils\platformRequest.ts 平台专用 axios 实例
   ├─ src\main.ts                 我在末尾加了两块「平台定制」
   └─ check-platform.cjs          我做的 SFC 编译校验脚本（不依赖 esbuild）
```

---

## 1. 怎么跑起来

### 后端（必须先起）

```powershell
cd D:\22project\testRestfulProject
venv\Scripts\python.exe main.py          # → http://127.0.0.1:5000
```

- 数据库配置在 `testRestfulProject/db.env`（服务启动自动读取，无需 export）：

```
MODEL_DB_DIALECT=mysql
MODEL_DB_HOST=127.0.0.1
MODEL_DB_PORT=3306
MODEL_DB_USER=ljx666
MODEL_DB_PASSWORD=123456
MODEL_DB_NAME=model_management
```

- 什么都不配 → 自动退回 SQLite 兜底库 `data/model_management.db`。
- 自检：浏览器开 http://127.0.0.1:5000/health

### 前端

```powershell
cd D:\22project\frontend\22project
npm run dev                              # → http://localhost:8080
```

- 打开后用**任意账号**登录（例如 `admin` / `123456`）——登录接口在 Flask 兼容层里，不校验口令。
- 登录后左侧菜单：**首页 / 模型管理 / 数据集管理 / 数据展示 / 系统管理**（菜单由后端 `GET /api/system/menu/web_router/` 下发）。

### 三个地址

| 地址 | 内容 |
|---|---|
http://127.0.0.1:5000/health | 后端体检（数据库、各表行数） |
http://127.0.0.1:5000/ui | 我早期做的**零构建单页控制台**（功能与 Vue 前端重叠，保留作备用入口） |
http://localhost:8080 | 正式的 Vue 前端 |

---

## 2. 后端 `model_service` 模块清单

| 文件 | 职责 |
|---|---|
`config.py` | 路径/数据库配置（环境变量覆盖）、`db.env` 读取、临时目录可写性自动回退 |
`datasets.py` | **数据源①：CWRU `.mat`**。类别表写死、滑窗切分、体检（whosmat + 120s 缓存）、`finalize_windows()`（两个数据源共用的标准化/划分） |
`tabular.py` | **数据源②：Excel/CSV 表格数据集**。读表（utf-8/GBK、xlsx via openpyxl）、列统计与预览、信号列自动识别、按"一文件一类别"切窗、体检带 TTL 缓存 |
`registry.py` | 模型产物落盘/加载（`data/models/<名>/<版本>/`），meta.json 读写，版本删除 |
`db.py` | 数据库层：**每线程复用连接**、`ensure_schema()` 幂等建表、按外键顺序写入、CRUD、引用检查、**行数带 30s 缓存** |
`training.py` | 训练编排：数据源解析 → 复用原脚本网络结构 → 训练 → 落盘 → 出图 → 写 `Trainings` |
`inference.py` | 推理：输入校验（长度/NaN/越界拒绝）→ 套用训练期 scaler → 三种框架分流 → 写 `InferenceTasks`+`InferenceResults`+`ModelInvocations` |
`figures.py` | 出图（matplotlib **Agg 后端**，落 PNG 不弹窗）：训练曲线、混淆矩阵、每类指标、预测分布、预测窗口波形 |
`api.py` | flask_restful 资源：业务接口 + CRUD + 上传 + `/ui` 控制台 |
`dvadmin.py` | **dvadmin 兼容层**：登录/用户信息/动态菜单/字典/设置/消息/验证码 + **CORS** + `/sse/` 桩 + 未实现 `/api/**` 的 JSON 404 |
`console.html` | 零构建单页控制台（`/ui`） |

---

## 3. 接口清单

### 3.1 业务接口（返回**裸 JSON**，不带信封）

| 方法 | 路径 | 作用 |
|---|---|---|
GET | `/health` | 服务/数据库/产物体检（各表行数带 30s 缓存） |
GET | `/models` | 产物清单 + 库表登记的模型（供列表用） |
GET | `/models/<名>` | 产物 `meta.json` 全文 |
GET | `/models/<名>/overview` | **模型档案合并接口**：登记信息 + 产物参数 + 指标 + 类别表 + 数据集指纹 + 混淆矩阵 + 最近训练 + 引用统计 |
GET | `/models/<名>/references` | 该模型被各表引用多少行（删除前体检） |
POST | `/models` | **增**：登记一条模型（ModelName 唯一键、Description、ModelType、ApiEndpoint、Status） |
PUT | `/models/<名>` | **改**：Description / ModelType / ApiEndpoint / Status / IsActive；带 `NewModelName` 时**连产物目录一起改名**（见 §7「改名」） |
DELETE | `/models/<名>?version=vN` | 删除产物版本（文件） |
DELETE | `/models/<名>?scope=record[&force=true]` | 删除 Models 表登记行（有引用默认 409 拒绝） |
POST | `/models/upload` | **上传模型**（multipart：`name`、`version?` + 多个 `file`，**文件夹或单个/多个文件都行**）。不再需要 `input_len/num_classes/labels`：服务端先判断「这是不是一个模型」，再从权重里读出输入长度与类别数；没有候选通过 → 400 且带 `detail` 逐个说明失败原因 |
GET | `/datasets` | 数据集体检（内置 .mat + `data/datasets` 下上传的表格数据集） |
GET/POST | `/datasets/db` | Datasets 表：查 / 登记 |
GET/PUT/DELETE | `/datasets/db/<id>` | Datasets 表：单行查 / 改 / 删 |
POST | `/datasets/upload` | 上传表格文件到 `data/datasets/<名>/`（multipart） |
GET | `/datasets/table` | 表格预览（列统计/前 N 行/推荐信号列） |
GET | `/datasets/signal` | 取一段信号（.mat 或表格，降采样数值数组） |
POST | `/train` | 训练 → 落盘 + 出图 + 写 `Trainings`（**同步阻塞**） |
GET | `/trainings?limit=N` | 训练记录（读库） |
POST | `/predict` | 推理 → 写 `InferenceTasks`/`InferenceResults`/`ModelInvocations` + 出图 |
GET | `/inference-tasks?limit=N` | 推理任务列表 |
GET | `/inference-tasks/<id>` | 任务 + 逐行结果 |
GET | `/figures?limit=N` | 已生成的图列表 |
GET | `/figures/<路径>` | 直接返回 PNG |
GET | `/system` | 运行信息（Python/依赖版本/路径/各表行数/占用） |
GET | `/system/logs`、`/system/logs/<名>?tail=N` | 训练日志列表 / 尾部内容 |
POST | `/system/maintenance` | 维护（`target=figures` 清空图库） |
GET | `/ui`、`/` | 零构建控制台 / 跳转 |
GET | `/api` | 业务接口索引 |

### 3.2 dvadmin 兼容接口（返回 `{code:2000, data, msg}` 信封）

| 方法 | 路径 | 说明 |
|---|---|---|
POST | `/api/login/` | 登录，任意账号；**必须返回 `pwd_change_count: 1`**（见 §8 坑 4） |
GET | `/api/system/user/user_info/` | 用户信息（管理员，roles=['admin']） |
GET | `/api/system/menu/web_router/` | **动态菜单**：4 个模块（首页由框架注入，见 §8 坑 6） |
GET | `/api/init/dictionary/`、`/api/init/settings/` | 字典（数组）/ 系统设置（对象） |
GET | `/api/system/menu_button/menu_button_all_permission/` | 按钮权限（数组） |
GET | `/api/system/dept/all_dept/`、`/dept_all/` | 部门树（**必须是数组**，见 §8 坑 3） |
GET | `/api/system/message_center/...`、`/api/captcha/`、`/api/system/system_config/...`、`/api/dvadmin3_social_oauth2/...` | 补齐启动依赖 |
GET | `/sse/` | 站内消息推送桩（返回 `retry: 3600000` 的一次性空事件流） |

---

## 4. 数据库（MySQL `model_management`，8 张表 + 14 条外键）

写入严格按外键顺序：`Datasets/Models → Trainings → InferenceTasks → InferenceResults`。

| 表 | 存什么 | 谁写 | 当前行数 |
|---|---|---|---|
`Datasets` | DatasetName(唯一)/Source/SampleCount/ClassCount/DataPath/Description | `/train` 自动登记、`POST /datasets/db`、`/predict` 的 ADHOC 登记 | 4 |
`Models` | ModelName(唯一)/Description/ApiEndpoint/ModelType/Status/IsActive/CreatedDate | `/train`、`POST /models`、`PUT /models/<名>` | **3**（1DCNN / cwt_cnn / adtk） |
`Trainings` | ModelID/DatasetID/TrainName/Epochs/BatchSize/Accuracy/Loss/**ModelPath**/Status/起止时间/Remark(JSON：跳过越界窗口、NaN、日志与图目录) | `/train`（失败也写一行） | 6 |
`ModelInvocations` | 请求/响应原文、耗时、状态码、ClientIP | 每次 `/predict` 一条 | 21 |
`ModelDeployments` | 版本/别名/环境/DeployUrl/端口/IsCurrent…（关联 Model×Training×EdgeDevice） | **未接入** | 0 |
`InferenceTasks` | **TrainingID(权威锚点)**/TargetDatasetID/TaskName/TaskType/Status/InferenceParams/ResultSummary/InputPath/**OutputPath(图目录)**/Progress | 每次 `/predict` | 21 |
`InferenceResults` | RowIdentifier(文件名#窗口)/SampleIndex/PredictedClass/Label/Confidence/Score/IsAnomaly/AnomalyScore/FeatureSnapshot/ActualClass/ResultDetail | 每条推理样本 | 68 |
`EdgeDevices` | 设备名/类型/IP/MAC/位置/硬件规格/心跳/状态 | **未接入** | 0 |

**设计取舍**：`InferenceTasks.TrainingID` 是权威锚点（不是 DeploymentID）；`TargetDatasetID` 为 NOT NULL，所以内联样本推理会登记一条 `ADHOC-<模型名>` 数据集。
**已知冗余**（原设计遗留，未清理）：`Models.Status` vs `IsActive`；`InferenceResults` 里 `PredictedClass/Label/Category` 与 `Score/AnomalyScore/PredictedValue` 近义；`EdgeDevices.EdgeStatus` vs `Status`。

---

## 5. 磁盘产物布局

```
data/models/<模型名>/<版本>/
    model.h5 | model.keras | model.pt | detector.pkl   权重
    scaler.npz                                          ★ 训练期标准化参数（推理必须复用）
    meta.json                                           ★ input_len/类别表/超参/指标/数据集指纹/混淆矩阵
data/figures/<模型>/<版本>/
    training_curves.png  confusion_matrix.png  per_class_metrics.png
    predict-<时间戳>/prediction_distribution.png  predicted_windows.png
data/logs/train-<模型>-<时间>.log                        训练全过程日志
data/datasets/<数据集名>/                                上传的 Excel/CSV（一文件一类别）
data/model_management.db                                 未接真库时的 SQLite 兜底库
db.env                                                   数据库连接配置
```

当前产物：`1dcnn/v1`（model.h5，acc 0.9457）、`cwt_cnn/v1`（model.pt，acc 0.6680）。

---

## 6. 两种数据源

### 6.1 CWRU `.mat`（数据源①）

- 目录：`1DCNN/0HP`（10 个文件 = 10 类：B007/B014/B021、IR007/IR014/IR021、OR007@6/OR014@6/OR021@6、normal）
- 取 **DE**（驱动端）通道；`scipy.io.loadmat`
- ⚠ `IR014` 只有 **63,788 点**（其他约 24 万），而 `number=600, stride=150, length=784` 要求切到 91,418 点 → 越界窗口被 **跳过并回报**（`strict=true`，绝不补 NaN）
- `cwt_cnn/0HP` 是同一批文件的**重复副本**（MD5 全同）

### 6.2 表格数据集（数据源②）

**约定：一个文件 = 一个类别，文件名即标签，表内指定一列作信号。**

- 支持 `.csv/.txt/.xlsx/.xls`；CSV 依次试 `utf-8-sig → utf-8 → gbk`；xlsx 走 openpyxl、xls 走 xlrd
- 信号列自动识别：列名像信号（振幅/振动/加速度/value/signal…）→ 首个有波动的数值列；**时间/序号列会被排除**；可用 `column` 显式覆盖
- 上传：前端「数据集管理 → 表格数据集」或 `POST /datasets/upload`，落到 `data/datasets/<名>/`
- 表格数据集演示数据：曾有一份**合成**数据 `data/datasets/DEMO-轴承表格数据/`（4 文件 4 类，故意混入"时间""温度"干扰列），**已删除**。它是为验证「一文件一类别 + 自动选信号列」这条通路而生成的模拟波形，不是真实采集数据，用它训出的 `test_accuracy=1.0` 不能当实验结果。
- 实测：`length=512, number=280, stride=128, epochs=30` → 测试准确率 **1.0000**、验证 0.9940

### 6.3 训练超参默认值

| 模型 | 数据源 | length | number | stride | rate | epochs | 其他 |
|---|---|---|---|---|---|---|---|
`1dcnn`（TF/Keras） | 两种都支持 | 784 | 600 | 150 | [0.7,0.15,0.15] | 10 | batch_size=128 |
`cwt_cnn`（PyTorch） | 两种都支持 | 784 | 300 | 150 | [0.5,0.25,0.25] | 50 | lr=1e-3，**全批量**（忽略 batch_size） |
`adtk`（无监督） | 仅 .mat 基线（默认 `normal_0_97.mat`） | 784 | 600 | 784 | — | — | detector=PcaAD，**k=4，c=5.0**，feature_mode=stats（10 维统计特征）或 raw（784 点原始幅值），sampling_rate=48000，threshold_quantile=0.995，factor=1.0 |

`/train` 常用参数：`model`、`epochs`、`batch_size`、`length`、`number`、`stride`、`rate`、`seed`、`strict`、`legacy_scaler`、`dataset`、**`dataset_type`(matlab/tabular)**、**`dataset_dir`**、**`signal_column`**。epochs 上限 200。

### 6.4 adtk（无监督）怎么用才对

`PcaAD` 内部就是 `PcaReconstructionError(k)` + `InterQuartileRangeAD(c)`，**它把 DataFrame 的每一行当成高维空间里的一个点**。所以喂进去的矩阵必须是「**行 = 一个窗口**」：

```
基线 normal_0_97.mat（243938 点）
  └─ 按 length=784 / stride=784 切 → 311 个窗口
       └─ 每个窗口 → 10 维特征（RMS、峭度、偏度、峰值因子 + 6 段频带能量比）   ← feature_mode=stats
            └─ DataFrame(311 行 × 10 列) → PcaAD(k=4, c=5).fit()   ← 这里的"行"就是样本点
                 ├─ PcaReconstructionError.transform() → 每个窗口一个连续分数
                 └─ 阈值 = 正常窗口分数的 99.5 分位（连同检测器一起存进 detector.pkl）

推理：窗口 → 同样的特征 → 重构误差 > 阈值 ? 异常 : 正常
```

反面教材（老代码，已修）：喂「采样点 × 1 列」的 DataFrame → 每行只有 1 维 → PCA 退化成恒等变换 → 重构误差恒为 0 → IQR 阈值只在数值噪声上乱响 → 判出来的"异常"跟故障毫无关系。

- `feature_mode=stats`（默认，10 维）与 `feature_mode=raw`（784 点直接当特征，不手工提特征）**实测都是 AUC = 1.0000**；想演示"原始信号端到端"就用 raw，代价是维度高、pkl 大一些。
- 频带按 `0..sampling_rate/2` **等分**，所以换采样率只要改 `sampling_rate`（12k 数据填 12000），不用改代码。
- 训练/推理共用 `training.adtk_window_features()`，改特征只改一处，不会两边走样。

---

## 7. 前端定制点（相对 dvadmin 原版改了哪些）

| 文件 | 改动 |
|---|---|
`.env`、`.env.development` | `VITE_API_URL` → `http://127.0.0.1:5000` |
`package.json` | `"dev": "vite --force"` → **`"vite"`**（去掉每次强制重建预构建缓存） |
`src/stores/themeConfig.ts` | `globalTitle/globalViceTitle` → **轴承故障诊断平台** |
`vite.config.ts` | 就地实现 `generateVersionFile()`，不再 import 带 `import.meta` 的模块（消掉 CJS 警告） |
`src/utils/menu.ts` | `handleMenu()` 注入的 `/home` 改为指向 `platform/home/index`、标题"首页"、`isKeepAlive: false` |
`src/stores/frontendMenu.ts` | 同上（另一套路由模式的 /home） |
**新增** `src/utils/platformRequest.ts` | 平台专用 axios 实例：**不校验 `{code,data,msg}` 信封**，直接返回 `response.data`（关键！见 §8 坑 1） |
**新增** `src/api/platform/index.ts` | 全部平台接口封装（含 CRUD、上传、overview） |
**新增** `src/views/platform/{home,model,dataset,visual,system}/index.vue` | 5 个模块 |
`src/main.ts`（末尾） | ① 清空持久化的路由缓存（`routesList`/`tagsViewRoutes`/`requestOldRoutes`/`keepAliveNames`）② 平台路由间跳转时 `window.location.reload()`（带防死循环标志位） |
**新增** `check-platform.cjs` | 不依赖 esbuild 的 SFC 编译校验（`node check-platform.cjs`） |

### 五个模块的功能

| 模块 | 内容 |
|---|---|
**首页** `/home` | 4 张 KPI（产物/训练/推理/结果明细）、服务与数据库、快捷入口、最近训练、最近推理任务 |
**模型管理** `/platform/model` | 清单（列：**模型/登记信息 · 产物 · 指标 · 操作**；「产物」只显示框架+版本（或"无产物"），「指标」分类看测试准确率、异常检测看基线误报率——**输入长度/类别数/权重这些参数细节只在下方的详情卡里显示，列表不再重复搬运**）、点行在**下方展示该模型的参数**（参数 + 超参，已按要求精简）、训练、推理、训练记录、推理任务（含明细）；工具栏两个按钮：**上传模型**（蓝色主按钮，独立弹窗，选文件夹或选文件，后端上传时自动登记 Models 行）、**刷新**；行操作只有**编辑 / 删除**（「新增模型」已按要求去掉——登记改由上传时自动完成，`POST /models` 接口仍在，需要时可恢复）。**详情卡按类型分两套**：分类/回归显示「类别数 + 类别标签个数」，异常检测显示「检测器(k) / 判定阈值 / 基线文件 / 基线误报率」并多一行无监督判定说明；推理结果表里那一列也从「异常占比」改名为「异常分数」（小数用科学计数法，不会把 `4.5e-5` 显示成 `0.0000`） |
**数据集管理** `/platform/dataset` | 数据集体检（按类型切换列）、表格数据集（上传 + 列表 + 预览列统计/前 N 行/切换信号列）、库表登记（增删改查） |
**数据展示** `/platform/visual` | 信号浏览（数据集/文件/信号列/起始点 + canvas 波形 + 统计）、图库（过滤 + 点击放大） |
**系统管理** `/platform/system` | 运行信息（Python/依赖/路径/占用）、数据库（连接 + 8 表行数）、日志（列表 + 尾部）、接口索引、维护（清空图库 / 删除模型版本，红色 + 二次确认） |

### 上传模型（文件夹，或单个/多个文件）

入口是工具栏的**「上传模型」独立弹窗**——新增/编辑登记弹窗里**不放上传控件**（否则两处长得一模一样，容易混）。
弹窗里两个按钮：「选择整个文件夹」（配 `<input webkitdirectory>`）和「选择模型文件（可多选）」（配 `<input multiple>`）；
两个原生 input 都设成 `display:none`，由按钮 `click()` 触发，所以界面上看到的是**两个有文字标签的按钮**，而不是两个一样的「选择文件」框。
两条路都 → `POST /models/upload`。

**① 判断「这是不是一个模型」**（`probe_weight()`，看内容不只看后缀）：

| 文件 | 判定规则 |
|---|---|
| `.h5` | HDF5，且含 `model_weights` 组或 `model_config` 属性（Keras 存档） |
| `.keras` | zip，且含 `config.json` / `metadata.json`（Keras 3 存档） |
| `.pt` `.pth` | zip 且含 torch 的 `data.pkl`；能 `torch.load(weights_only=True)` 就顺手把参数读出来 |
| `.pkl` `.pickle` | pickle（`0x80` 开头）——**不反序列化**，避免「上传即执行代码」 |
| 其它 | 直接判「不是模型」，失败原因逐个回给前端（响应里的 `detail`） |

**② 自动识别参数**（所以新增/编辑弹窗里**不再有**输入长度/类别数/类别标签）：Keras 解析 `model_config` → `input_len` = 输入层形状、`num_classes` = 最后一个 Dense 的 `units`；PyTorch 从 `fc.weight`/`classifier.weight` 这类键读类别数；10 类自动补 CWRU 中文标签；真读不出 `input_len` 就写进响应 `warnings`（推理侧按默认 784），**不再 400 拒绝**。

**③ 落盘**：`data/models/<名>/vN/`；带 `meta.json` 就以它为准（只把缺失的 `input_len/num_classes` 补齐）；白名单外文件跳过并回报；任何异常 **rmtree 回滚**；版本冲突 409。

### 改名

`PUT /models/<名>` 传 `NewModelName` 就能改名，一次要同步四处，少一处就留死链：

1. 产物目录 `data/models/<老名>` → `data/models/<新名>`（`_rename_model()`，同盘 rename 秒完成）
2. 各版本 `meta.json` 里的 `model` 字段（无条件改写）
3. 库里已存的路径：`Trainings.ModelPath`、`InferenceTasks.InputPath/OutputPath`、`ModelDeployments.DeployedPath/DeployUrl`（SQL `REPLACE()`，见 `db.rename_model_paths()`）
4. `Models.ModelName`（先查重，重名返回 409；改库失败自动 `_undo_rename()` 把目录搬回去）

**注意**：内置三个模型（`1DCNN`/`cwt_cnn`/`adtk`）的名字在 `training.py` 的 `MODEL_META` 里是**硬编码的训练模板**，改名后**不能再用新名字训练**（用老名字训练会重新建出老目录），但看档案 / 推理照常——接口会在响应 `rename.note` 里说明这一点。

---

## 8. 踩坑记录（都是实际发生并已修的）

| # | 现象 | 根因 | 修法 |
|---|---|---|---|
1 | 前端所有业务请求报 `非标准返回：[object Object]， /models` | dvadmin 的 `utils/service.ts` 拦截器**强制校验 `{code,data,msg}` 信封**，而我的接口是裸 JSON | 新增 `utils/platformRequest.ts` 专用 axios 实例，平台接口不走框架的 service |
2 | 部署点不了：`/api/**` 返回 **405** | 我给 CORS 加的 `OPTIONS /api/<path:_any>` 兜底路由**参与 URL 匹配**，把未注册的 `/api/xxx` 截成 405 | 删掉兜底路由，改用 `@app.errorhandler(404)` 返回 JSON |
3 | `Cannot create property 'id' on number '0'` | `/api/system/dept/all_dept/` 我返回 `{results,total}`，前端要**数组**（`XEUtils.toArrayTree`） | 改成数组 + 补 `dept_all`、`menu_button_all_permission`、`init/settings`、第三方登录列表 |
4 | **登录后不跳转** | 登录响应缺 `pwd_change_count`：前端 `loginSuccess()` 只有 `if (pwd_change_count > 0) router.push()` 一个分支 | 登录响应带上完整用户字段 + `pwd_change_count: 1` |
5 | 模型推理报"未知模型"（上传的模型名） | `normalize_model()` 只认 1dcnn/cwt_cnn/adtk 三个别名，且 API 层与推理层各拦一次 | API 层放行 + `_artifact_key()` 兼容任意模型名 |
6 | **两个「首页」** | 首页被**两个来源各注入一次**：`utils/menu.ts` 与 `stores/frontendMenu.ts`；早期我的后端菜单也下发过一个 `/platform/home`（`isAffix=true`，标签无 ×，关不掉，且被持久化） | 已删后端菜单里的首页、两个来源的 `/home` 都指向本平台首页；`main.ts` 启动时清空持久化路由缓存（**见 §10 遗留**） |
7 | 页面切回来数据不刷新 | 菜单配成 `cache: true` → KeepAlive 缓存，切回不重新挂载 | 菜单 `cache: False` + 两个 `/home` 的 `isKeepAlive: false`；`main.ts` 再加"切页整页刷新" |
8 | 模型上传 500 | `api.py` 缺 `import json` | 补 import |
9 | 上传缺 `input_len` 时目录残留 | 分支里没真回滚 | 补 `shutil.rmtree` |
10 | 模型保存失败（受限环境） | 本机沙箱禁止在 `mkdtemp` 目录里写文件 → Keras 原生 `.keras`（先写临时文件再改名）必失败；失败的保存会留下**只有 config.json 没有权重**的半成品 zip，被权重查找误命中 | 自动回退 h5py 直写的 `.h5` + 显式删除半成品 + `meta.json` 记录 `weights_file` |
11 | 页面上写库名大小写不一致 | 服务用 `1dcnn`、种子数据是 `1DCNN` → Models 表插出重复行 | 统一走 `db_model_name()` |
12 | `/trainings` 接口 500 | pymysql 回读 `DATETIME` 返回 `datetime` 对象，Flask JSON 编码器不认 | `_rows_to_dicts` 统一归一化 datetime/Decimal/bytes |
13 | **页面跳转很慢** | ① 每个请求都新建 MySQL 连接 ② `/health`、`/system` 每次真跑 8 条 `COUNT(*)` ③ `/datasets` 用 `loadmat` 把 24 万点数组整个读进内存（10 个文件），表格数据集每次真读 4 个 CSV/xlsx | ① 连接按线程复用 ② 行数缓存 30s ③ 改 `whosmat` 只读形状 + 体检结果 TTL 缓存 120s |
14 | 环境限制：装不了包 / 跑不了 vite | pip 与 esbuild 都需要写临时目录或起子进程走管道，被沙箱拒绝（`EPERM`） | 依赖用"下载 wheel 手动解包到 site-packages"安装（pymysql、pyodbc、openpyxl、xlrd、et-xmlfile）；前端用 `check-platform.cjs` 做等价编译校验 |
15 | 上传模型必须手填「输入长度/类别数/类别标签」 | 没有 `meta.json` 时推理侧不知道 `input_len`，接口就把它设成必填，把负担推给了用户 | 改成**内容探测**：`probe_weight()` 判断是不是模型，Keras 读 `model_config`、PyTorch 读 `fc.weight` 形状，参数自己读出来；读不到就 `warnings` 说明而非拒绝 |
16 | 改模型名会让「库表」和「磁盘」脱钩 | `ModelName` 同时是产物目录名、`meta.json` 的 `model` 字段、`Trainings.ModelPath` 里的路径片段——只改库必留死链 | `PUT /models/<名>` 带 `NewModelName` → `_rename_model()`（搬目录 + 改 meta）+ `rename_model_paths()`（SQL `REPLACE()` 换库里路径），改库失败自动 `_undo_rename()` 搬回去 |
17 | 改名后 `meta.json` 里的 `model` 没跟着变 | 上传时 meta 写的是用户输入的 `safe`（可能含大写），改名比对用的却是 `_artifact_key()`（小写）；Windows 路径大小写不敏感，所以只在 meta 内容上暴露 | 改名时**无条件**改写目录内所有 `meta.json` 的 `model` 字段，不再做相等判断 |
18 | **adtk 判出来的"异常"跟故障无关**（实测故障窗口异常占比 0.026~0.103 < 正常 0.137，方向是反的） | `PcaAD` 内部是 `PcaReconstructionError(k)` + `InterQuartileRangeAD(c)`，它把 **DataFrame 的每一行**当作高维空间里的一个点；老代码喂的是「采样点 × 1 列」，PCA 退化成 1 维、重构误差恒为 0，靠 IQR 阈值只在噪声上乱响 | 改成**行 = 窗口**：基线信号按 `length/stride` 切成 (number, 784) 或 (number, 10) 矩阵再喂；连续分数用 `PcaReconstructionError.transform()`，阈值取正常窗口分数的 99.5 分位。**实测 AUC 1.0000，正常文件 0/20 判异常、4 个故障文件 19~20/20 判异常** |

**性能数字（实测）**：`/health` 250ms → **48ms**；`/datasets` 2035ms → **50ms**（首次仍约 2s，缓存进程内、重启失效）；其余接口 40~100ms。

---

## 9. 当前数据状态（清理后）

```
MySQL（数值会随每次训练/推理增长，下面是最近一次核对值）：
       Datasets 6 ｜ Models 3（1DCNN / cwt_cnn / adtk）｜ Trainings 8
       InferenceTasks 26+ ｜ InferenceResults 168+ ｜ ModelInvocations 26+
       ModelDeployments 0 ｜ EdgeDevices 0
       （其中若干 Trainings / InferenceTasks 行是开发期自测留下的，可安全清理）
磁盘：data/models/{1dcnn,cwt_cnn,adtk}/v1（真实训练的产物，adtk 是按新方式重训的）
      data/datasets/（当前为空：合成演示数据已删除，上传的表格数据集会落在这里）
      data/figures/**（训练/推理自动出的图）
```

**adtk 现在真的可用了**（之前只有库表登记、磁盘没产物）：`data/models/adtk/v1/{detector.pkl,meta.json}`，
产物里 `params = {detector: PcaAD, k: 4, c: 5.0, feature_mode: stats, sampling_rate: 48000, length: 784, number: 600, stride: 784, threshold_quantile: 0.995, factor: 1.0, baseline_file: normal_0_97.mat}`，
`metrics.threshold = 0.002003`、`metrics.baseline_false_positive_rate = 0.0064`、`metrics.score_median = 0.000146`。
实测：正常文件 0/20 窗口判异常，内圈/外圈/滚动体 3 类故障文件各 19~20/20 判异常（相对正常分数 30~7869 倍）。

**已清理**：`data/models/1dcnn/v2,v3`（表格数据测试产物）、MySQL 里 `CRUD临时模型` 登记行、`D:\22project\22project`（半份副本）。

**残留（都是早期试上传留下的，没登记进 Models 表，可随时删）**：
`data/models/UPLOAD测试模型/`（空目录）、`data/models/缺参数模型/v1/`（没测出输入长度的产物）。
删除命令：`Remove-Item -Recurse -Force D:\22project\testRestfulProject\data\models\UPLOAD测试模型, D:\22project\testRestfulProject\data\models\缺参数模型`

---

## 10. 已知遗留问题（下次接手从这里开始）

| 优先级 | 问题 | 现状与下一步 |
|---|---|---|
**高** | **标签栏仍是两个「首页」** | 左侧菜单已正常（只有一个），重复出现在**顶部标签栏**且是 `isAffix`（无 × 关不掉）。根因是浏览器里**持久化的旧路由**（早期后端菜单下发过 `platformHome`，日志 `parent.vue:62` 的实时路由名里仍有它）。我已加"启动时清空 `routesList`/`tagsViewRoutes` 缓存"的代码，但**日志里没有出现我加的那行 `[platform] 已清空缓存的路由数据：…`**，说明前端 bundle 没更新（dev server 未真正重启 / 页面未硬刷）。**下一步**：① 确认 `npm run dev` 重启过；② **Ctrl+Shift+R**；③ 若仍不行 → F12 → Application → **Clear site data** 后重新登录；④ 若清完还有两个，就是"实时注入"，需删掉 `utils/menu.ts` 或 `stores/frontendMenu.ts` 里那段静态 `/home`（**只能删一处，删错会没有首页**） |
中 | `SSE 首次连接成功 → SSE 错误` | `/sse/` 桩是一次性空流，连上即关闭，EventSource 按规范报一次 error。要彻底安静就把它改成 30s 心跳的长连接 |
中 | 切页整页刷新的代价 | `main.ts` 里那块 ② 会让每次切模块重新初始化整个后台（dev 模式 3~10 秒）。满意后想关掉就删掉标注 `②` 的 10 行 |
中 | `ModelDeployments`、`EdgeDevices` 未接入 | 表与 UI 都留着位置，对应流程图里"部署 → 边缘设备 → 机床"支线 |
低 | 无 `requirements.txt` / 无 git / 无 README（根目录） | 依赖只存在于 `testRestfulProject/venv`（2.7GB）。本文件即为交接文档 |
低 | 无法删除的目录 | `D:\22project\.piptmp`、`testRestfulProject\data\tmp`、`data\.tmpcheck`（沙箱锁着 mkdtemp 子目录），需在普通终端 `rd /s /q` |
低 | 原项目残留 | `main.py` 的 `PcaAD` 死代码与 `TODOS` 示例、`1DCNN/preprocessing.py` 从未调用的 `filter_by_most_common_shape`、`adtk/main.py` 大段注释、`createdoc.py`（与本平台无关） |
低 | `console.html` + `/ui` | 零构建控制台，功能被 Vue 前端覆盖，保留作备用入口 |

---

## 11. 常用命令速查

```powershell
# 起后端 / 前端
cd D:\22project\testRestfulProject; venv\Scripts\python.exe main.py
cd D:\22project\frontend\22project; npm run dev

# 前端页面编译校验（不需要 esbuild / dev server）
node D:\22project\frontend\22project\check-platform.cjs

# 后端自测：模型探测（是不是模型）+ 上传（免填 input_len）+ 改名，跑完自动清理
cd D:\22project\testRestfulProject; venv\Scripts\python.exe _selftest_upload.py

# 训练（命令行）
curl -X POST http://127.0.0.1:5000/train -H "Content-Type: application/json" -d '{"model":"1dcnn","epochs":10}'
# 训练 adtk（无监督异常检测：窗口当样本 + adtk 的 PCA 重构误差）
curl -X POST http://127.0.0.1:5000/train -H "Content-Type: application/json" -d '{"model":"adtk","k":4,"feature_mode":"stats","threshold_quantile":0.995}'
# adtk 推理（正常文件应当判"正常"，故障文件应当判"异常"）
curl -X POST http://127.0.0.1:5000/predict -H "Content-Type: application/json" -d '{"model":"adtk","path":"1DCNN/0HP/48k_Drive_End_IR007_0_109.mat","index":0,"limit":5}'
# 用表格数据集训练
curl -X POST http://127.0.0.1:5000/train -H "Content-Type: application/json" -d '{"model":"1dcnn","dataset_type":"tabular","dataset_dir":"data/datasets/<你的数据集目录名>","length":512,"number":280,"stride":128,"epochs":30}'
# 推理（按窗口）
curl -X POST http://127.0.0.1:5000/predict -H "Content-Type: application/json" -d '{"model":"1dcnn","path":"1DCNN/0HP/normal_0_97.mat","index":0,"limit":3}'
# 模型档案（登记信息+参数+指标+类别表+最近训练+引用）
curl http://127.0.0.1:5000/models/1dcnn/overview
# 数据库直连
& D:\22project\testRestfulProject\venv\Scripts\python.exe -c "import pymysql;c=pymysql.connect(host='127.0.0.1',user='ljx666',password='123456',database='model_management');print(c.cursor().execute('select 1'))"
```

---

## 12. 环境与依赖（`testRestfulProject/venv`，Python 3.14.7）

| 类别 | 版本 |
|---|---|
深度学习 | tf-nightly 2.22.0.dev + keras-nightly 3.16.0.dev ｜ torch 2.14.0+cpu |
数据/科学计算 | numpy 2.5.2、pandas 3.0.5、scipy 1.18.1、scikit-learn 1.9.1、matplotlib 3.11.2、h5py 3.15.1 |
Web | Flask 3.1.3、Flask-RESTful 0.3.10、Werkzeug 3.1.8 |
数据库驱动 | pymysql 1.2.0（**手动解包安装**）、pyodbc 5.3.0（同）、adodbapi（pywin32 自带） |
表格读取 | openpyxl 3.1.5、xlrd 2.0.2、et-xmlfile 2.0.0（**均手动解包安装**） |
其他 | pywin32 312、python-docx 1.2.0、statsmodels 0.15.0、requests 2.34.2 |

- 本机数据库：**MySQL 9.2**（`MySQL92` 服务，127.0.0.1:3306）应用账号 `ljx666/123456`（我创建的，已授权 `model_management.*`）；`root/123456` 也可用。
- SQL Server Express（`MSSQL$SQLEXPRESS`）虽在运行，但本机只有过时的 ODBC 驱动，**SQL Server 那一路跑不通**。
