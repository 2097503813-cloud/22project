# 项目技术详解（数据集 · 四个模块 · Bug 清单）

> 配套文档：[`项目交接文档.md`](项目交接文档.md)（怎么跑、接口清单、踩坑）· [`README.md`](README.md)（总览）
> 本文回答四件事：**数据集是什么格式** → **每个模型要什么格式** → **训练/推理/训练记录/推理任务各自怎么跑** → **哪里还有坑**。

---

## 1. 数据集清单（磁盘上的真实情况）

项目里一共有 **3 个数据来源**，其中前两个是"能用"的，第三个只是 adtk 的兜底示例。

| # | 数据集 | 磁盘位置 | 形态 | 规模 | 性质 |
|---|---|---|---|---|---|
| ① | **CWRU 0HP（内置）** | `testRestfulProject\1DCNN\0HP\` | 10 个 `.mat` | 每文件 12.7 万~49.9 万点 | 只读，代码里写死路径 |
| ② | **表格数据集（可上传）** | `testRestfulProject\data\datasets\<数据集名>\` | `.csv/.txt/.xlsx/.xls` | 任意 | 前端/接口上传 |
| ③ | adtk 自带示例 | `testRestfulProject\adtk\dataset\` | 14 个 `.csv` | 几百~几千行 | 只在找不到 `.mat` 基线时兜底 |

数据库 `Datasets` 表里现在有 6 行：`CWRU-0HP`、`CWRU-0HP(normal基线)`（adtk 训练时另建的）、`ADHOC-*`（推理时自动登记）、以及上传的表格数据集 —— **① 和 ② 都会在这里登记**。

### 1.1 数据集①：CWRU `.mat`（10 类，文件名即类别）

`1DCNN\0HP\` 下就是这 10 个文件，`datasets.py` 的 `CWRU_0HP_CLASSES` 把"文件名 → class_id → 中文标签"写死（顺序沿用最初 `os.listdir` 的字母序，所以和既有实验结果对得上）：

| class_id | 文件名 | 中文标签 | 文件点数 |
|---|---|---|---|
| 0 | `48k_Drive_End_B007_0_122.mat` | 滚动体故障-0.007in | 244,739 |
| 1 | `48k_Drive_End_B014_0_189.mat` | 滚动体故障-0.014in | 249,146 |
| 2 | `48k_Drive_End_B021_0_226.mat` | 滚动体故障-0.021in | 243,938 |
| 3 | `48k_Drive_End_IR007_0_109.mat` | 内圈故障-0.007in | 243,938 |
| 4 | `48k_Drive_End_IR014_0_174.mat` | 内圈故障-0.014in | **63,788** ⚠️ |
| 5 | `48k_Drive_End_IR021_0_213.mat` | 内圈故障-0.021in | 244,339 |
| 6 | `48k_Drive_End_OR007@6_0_135.mat` | 外圈故障-0.007in@6点钟 | 243,538 |
| 7 | `48k_Drive_End_OR014@6_0_201.mat` | 外圈故障-0.014in@6点钟 | 245,140 |
| 8 | `48k_Drive_End_OR021@6_0_238.mat` | 外圈故障-0.021in@6点钟 | 246,342 |
| 9 | `normal_0_97.mat` | 正常 | 243,938 |

- **读取方式**：`scipy.io.loadmat` 只取 **DE（驱动端）通道**（`datasets.read_de_channel()`）。
- **采样率 48 kHz**（文件名里的 `48k`），所以时间窗 784 点 ≈ 16 ms。
- 文件名不在表里的 `.mat` 会被 `guess_label()` 按文件名猜标签、`class_id=None`，**不会报错**。

### 1.2 数据集②：表格数据集（一文件一类别）

约定：**一个文件 = 一个类别，文件名（去扩展名）就是标签**，表内**自选一列**当振动信号。

```
testRestfulProject\data\datasets\<数据集名>\        ← 上传的表格数据集（原来这里是合成演示数据 DEMO-轴承表格数据，已删除）
├─ 正常.csv          列：时间,振动幅值,温度   （40000 行）
├─ 内圈故障.csv
├─ 外圈故障.csv
└─ 滚动体故障.xlsx
```

- 支持后缀：`.csv .txt .xlsx .xlsm .xls`（`tabular.TABLE_SUFFIXES`）；编码自动回退（UTF-8 失败试 GBK）。
- **选列规则**（`pick_signal_column()`，优先级从高到低）：
  1. 显式指定 `signal_column`；
  2. 列名命中信号词（`振幅/振动/幅值/加速度/signal/value/amplitude/vibration`）且**不**命中时间词（`时间/时刻/序号/time/date/index/no.`）→ `振动幅值` 会被选中，`时间`、`温度` 被跳过；
  3. 退一步：第一个"有波动且不像时间列"的数值列；
  4. 再退：第一个数值列。
- （原来那份合成演示数据里的 `温度` 列就是**故意的干扰项**，用来验证选列逻辑；该数据已删除，但选列规则不变。）

---

## 2. 三个模型分别需要什么格式

| | `1DCNN`（Keras 分类） | `cwt_cnn`（PyTorch 分类） | `adtk`（无监督异常检测） |
|---|---|---|---|
| 数据源 | ① `.mat` **或** ② 表格 | ① `.mat` **或** ② 表格 | 只要 **一段"正常"信号**（默认 `normal_0_97.mat`，缺失时退化用 ③ 示例 CSV） |
| 数据集的"类别"概念 | 必需（10 类，标签有序） | 必需 | **不需要**（只有"正常"基线） |
| 切窗 | `length=784`，`number=600`，`stride=150` | `length=784`，`number=300`，`stride=150` | `length=784`，`number=600`，`stride=784`（不重叠） |
| 训练/验证/测试划分 | `rate=[0.7,0.15,0.15]` | `rate=[0.5,0.25,0.25]` | 无（不是监督任务） |
| 归一化 | 是，`scaler.npz`（StandardScaler/自定义） | 是，`scaler.npz` | **无**（特征自带量纲无关性） |
| 输入张量形状 | `(n, input_len, 1)` | `(n, 1, input_len)` | 每个窗口 → 10 维特征向量 |
| 产物 | `model.h5` + `scaler.npz` + `meta.json` | `model.pt` + `scaler.npz` + `meta.json` | `detector.pkl` + `meta.json` |
| 输出 | 类别 + 置信度 + top_k | 类别 + 置信度 + top_k | `is_anomaly` + `anomaly_score`（PCA 重构误差） |

**上传模型**时（`POST /models/upload`）不需要用户填这些参数：服务端会看文件内容判断"是不是模型"，
Keras 从 `model_config` 读输入形状与类别数、PyTorch 从 `fc.weight` 形状读类别数。

---

## 3. 训练模块（`POST /train`）

### 3.1 全流程（入口 `training.train()`）

```
POST /train {model, epochs, length, number, stride, rate, dataset_type, dataset_dir, ...}
  │
  ├─1 api.Train：参数校验（epochs 上限 200；模型名走 normalize_model 的别名表）
  ├─2 training.train()
  │    ├─ 建日志文件 data/logs/train-<模型>-<时间戳>.log，把 stdout 重定向进去
  │    ├─ _TRAINERS[name](options)                          ← 三个 trainer 之一
  │    │    ├─ resolve_source(opts)   判定数据源（目录里有表格→tabular，否则有 .mat→matlab）
  │    │    ├─ load_dataset(...)      读 → 切窗 → 归一化 → 划分 train/valid/test
  │    │    ├─ 训练（Keras / PyTorch / adtk.fit）
  │    │    ├─ 评估：test_accuracy / loss / classification_report / 混淆矩阵
  │    │    └─ 返回 {framework, task, input_len, num_classes, labels, dataset_*, params, metrics, saver}
  │    ├─ registry.save_artifact()  → data/models/<模型名>/vN/{权重, scaler.npz, meta.json}
  │    ├─ figures.training_figures() → data/figures/**.png（失败不影响训练结果，只在响应里回报）
  │    └─ 写库：Datasets → Models → Trainings（严格按外键顺序）
  └─ 返回 {status, artifact, metrics, params, dataset_stats, figures, db}
```

### 3.2 三个 trainer 的差异

| | `_train_1dcnn` | `_train_cwt_cnn` | `_train_adtk` |
|---|---|---|---|
| 网络来源 | 复用原项目 `1DCNN/1DCNN.py` 的结构 | 原项目的 PyTorch 版（含 CWT 思路） | adtk 库的 `PcaAD(k, c)` |
| 关键超参 | 10 轮、batch 128 | 50 轮、lr=1e-3、**全批量**（忽略 batch_size） | k=4、c=5、`feature_mode=stats` |
| 训练集指标 | `history.accuracy/loss/val_*` | `history.loss`（无 val） | 无（只算基线误报率） |
| 主要指标 | `test_accuracy` | `test_accuracy` | `baseline_false_positive_rate` + `threshold` |

**adtk 的特别之处**：它把"一个窗口"当高维空间里的一个点（行 = 窗口），fit 只用正常基线，
推理时算每个窗口的 PCA 重构误差，`> 阈值` 判异常 —— 详见交接文档 §6.4。

### 3.3 产物与版本

```
data/models/<模型名>/
├─ v1\
│  ├─ model.h5 | model.pt | detector.pkl     ← 权重（meta.json 的 weights_file 指明是哪个）
│  ├─ scaler.npz                             ← 分类模型有，adtk 没有
│  └─ meta.json                              ← 全部元信息（下表）
└─ v2\ ...                                   ← 同名再训练自动取下一个 vN
```

`meta.json` 字段与读写方：

| 字段 | 谁写 | 谁读 |
|---|---|---|
| `model` / `version` / `framework` / `task` | `save_artifact` | 列表、推理分框架、改名 |
| `input_len` | trainer | **推理切窗**（缺失则退回 784） |
| `num_classes` / `labels` | trainer | 前端类别表、`actual_class` 映射 |
| `dataset{name,path,stats}` | trainer | 模型档案、前端数据集页 |
| `params` | trainer | 前端「超参」表 |
| `metrics` | trainer | 列表「指标」列、模型档案 |
| `confusion` | trainer | 出图、`/models/<名>` |
| `weights_file` / `scaler_file` | `save_artifact` | `_find_weights()`、推理加载 |
| `trained_at` / `log_file` / `created_at` | `train()` | 追溯 |

---

## 4. 推理模块（`POST /predict`）

### 4.1 全流程（入口 `inference.predict()`）

```
POST /predict {model, path|samples, index, limit, version, training_id, top_k, column, sheet}
  │
  ├─1 解析模型名：别名表优先，不在表里就按原名当产物目录名（支持上传的模型）
  ├─2 load_artifact()           读最近版本（或指定 version）的权重 + meta
  ├─3 input_len = meta.input_len 或 默认 784        ← 注意这条静默回退
  ├─4 _build_matrix()           文件 → 信号 → 切窗；或直接用内联 samples
  ├─5 _validate()               长度必须等于 input_len；出现 NaN/Inf 直接拒收
  ├─6 _DISPATCH[framework]      分框架预测
  │     ├─ _predict_keras  (n, L, 1) → softmax → 类别+置信度+top_k
  │     ├─ _predict_torch  (n, 1, L) → softmax → 同上
  │     └─ _predict_adtk   窗口→10 维特征→PCA 重构误差→与阈值比
  ├─7 补 actual_class：输入文件名命中 CWRU 十个文件之一时，填上真实类别
  ├─8 汇总 summary（分类：各类计数 + 是否全对；异常检测：异常窗口数/占比）
  ├─9 出图 figures.inference_figures()
  └─10 落库 _write_db()：InferenceTasks → InferenceResults → ModelInvocations
```

### 4.2 三个模型的输出字段

| 字段 | Keras | PyTorch | adtk |
|---|---|---|---|
| `index` | ✅ 窗口序号 | ✅ | ✅ |
| `predicted_class` | ✅ 0-9 | ✅ | ✅ 1=异常/0=正常 |
| `predicted_label` | ✅ 中文标签 | ✅ | ✅ `异常`/`正常` |
| `confidence` | ✅ | ✅ | — |
| `top_k` | ✅ | ✅ | — |
| `is_anomaly` | — | — | ✅ |
| `anomaly_score` | — | — | ✅ PCA 重构误差 |
| `actual_class` | ✅（仅 CWRU 文件） | ✅ | ✅ |
| `detail` | 类别总数 + 第二名概率 | 同左 | 重构误差/判定阈值/正常中位/倍数 |

### 4.3 落库细节（`_write_db`）

- **锚点**：`InferenceTasks.TrainingID` 是 NOT NULL 外键。不传 `training_id` 时用 `latest_training(模型, only_success=True)`；
  一次成功训练都没有 → 不写库并**在响应里明确说明原因**（不是静默失败）。
- **临时数据集**：内联/文件推理都会 `ensure_dataset("ADHOC-<模型名>")`，避免伪造真实数据集。
- **结果行**：每个窗口一行 `InferenceResults`（`SampleIndex` / `PredictedClass` / `PredictedLabel` / `Confidence` / `AnomalyScore` / `IsAnomaly` / `PredictedCategory` / `FeatureSnapshot` / `ResultDetail` / `ActualClass`）。
- **调用日志**：最后写一条 `ModelInvocations`（含耗时、状态码、请求参数、响应摘要）。

---

## 5. 训练记录（`Trainings` 表 / 列表页「训练记录」tab）

`GET /trainings?limit=N` → `db.recent_trainings()`：

```sql
SELECT t.TrainingID, t.TrainName, t.Epochs, t.BatchSize, t.Accuracy, t.Loss, t.Status,
       t.ModelPath, t.StartedDate, t.CompletedDate, t.CreatedDate, m.ModelName, d.DatasetName
FROM Trainings t LEFT JOIN Models m ON m.ModelID = t.ModelID
LEFT JOIN Datasets d ON d.DatasetID = t.DatasetID
ORDER BY t.TrainingID DESC LIMIT N
```

- **写入时机**：每次 `/train` 都写一行，**包括失败**（`Status='失败'`，`Accuracy/Loss` 为 NULL，`Remark` 里存 JSON：错误、跳过越界窗口数、NaN 窗口数、日志路径、图目录）。
- `ModelPath` 存的是**权重文件的绝对路径**（`data/models/<名>/vN/model.h5`），模型改名时会被同步替换前缀。
- `Accuracy` 是**测试集准确率**；adtk 恒为 NULL（无监督没有准确率）。
- 前端表格固定显示：ID / 模型 / 数据集 / 训练名 / 轮次 / 准确率 / loss / 状态 / 开始时间。

---

## 6. 推理任务（`InferenceTasks` + `InferenceResults` + `ModelInvocations`）

「推理任务」tab 展示 `GET /inference-tasks?limit=N`（`db.recent_inference_tasks()`）：每次 `/predict` **都会新建一个任务**（没有"批量任务"概念），字段：

| 字段 | 取值 |
|---|---|
| `TaskName` | `predict-<模型>-<时间戳>` |
| `TaskType` | 产物 meta 的 `task`（`classification` / `anomaly_detection`） |
| `Status` / `Progress` | 成功即写 `成功` / `100`（同步执行，没有中间态） |
| `TrainingID` | 锚点训练（见 4.3） |
| `TargetDatasetID` | `ADHOC-<模型>` 数据集 |
| `ResultSummary` | JSON：样本数、模型版本、输入来源、锚点训练、预测分布 |
| `InputPath` / `OutputPath` | 输入文件路径 / 本次出的图路径 |

点「明细」→ `GET /inference-tasks/<id>` → `db.inference_task()` 一次返回任务 + 全部 `InferenceResults` 行。

---

## 7. Bug 清单（三路代码审计汇总）

审计方式：三个只读审计员分别精读「数据集」「训练」「推理+记录+任务」，逐条落到 `文件:行号`。
级别：**高**=会导致错误结论/数据损坏/安全问题；**中**=可用但会误导或静默失真；**低**=整洁性。

### 7.1 已修（本轮）

| # | 级别 | 位置 | 问题 | 修法 |
|---|---|---|---|---|
| 1 | 高 | `registry._model_root` | 模型名直接拼进路径且**不做净化**，`model="../../x"` 可加载工作区外的产物，推理时还会 `pickle.load` → 任意文件读 / RCE | 加白名单正则（中英文数字 `_-.`）+ 拒绝 `..`，非法名抛 `ValueError`→400 |
| 2 | 高 | `inference._predict_keras` / `_predict_torch` | 用 `meta["input_len"]` **硬下标**，上传模型没写 `input_len` 时 `KeyError/TypeError` → 500，而接口文档承诺"按默认 784 处理" | 改成 `meta.get("input_len") or matrix.shape[1]` |
| 3 | 高 | `api.Predict` | `index` 允许负数：Python 负索引切片会切出**错位窗口**却通过校验（`SampleIndex` 写成负数）；`limit`/`top_k` 用 `_int(...) or 默认值`，**0 是 falsy**，`limit=0` 被悄悄改成 1 | 显式 `is None` 判断 + 范围校验，非法一律 400 |
| 4 | 高 | `api.DatasetSignal` | `dataset` 参数未净化，`?dataset=..\..\..\x` 可读**工作区外**文件（校验的是逃逸之后的目录） | 先 `directory.resolve().relative_to(workspace_dir)` 再取文件 |
| 5 | 中 | `training._train_adtk` | `baseline_file` 未校验（可带路径穿越）；找不到基线时**静默**退回 `adtk/dataset/cpu.csv`，拿 CPU 电流数据训出"轴承异常模型" | 只允许文件名 + 找不到就明确报错，不再静默换源 |
| 6 | 中 | `api.Train` | 只捕 `InvalidInput`：`FileNotFoundError`/`ValueError`（未知模型名、rate 非法、目录不存在）都变成 **500 HTML**，前端拿不到 JSON | 补 `FileNotFoundError`→409、`ValueError`→400；`dataset_dir` 的 `startswith` 前缀校验改为 `relative_to`（`D:\22project_evil` 不再能绕过） |
| 7 | 中 | `inference._write_db` | `feature_snapshot` 用 `json.dumps(...)[:500]` 按字符截断，中文列名时几乎必然切出**半截 JSON** | 先序列化再判断长度，超长写 `{"truncated": true}` |
| 8 | 中 | `inference._predict_adtk` | 产物缺 `threshold`（或为 0）时兜底成 `0.0` → **所有窗口都判异常** | 缺失/非法直接报错，要求重训 |
| 9 | 低 | `main.py` | 残留 flask_restful 官方示例（`TODOS`/`/todos`/`Todo`/`TodoList`/`PcaAD` 导入），与本平台无关 | 删除，只保留"注册两层路由 + 启动" |
| 10 | 低 | `data/models/缺参数模型`、`UPLOAD测试模型`、`data/tmp/probe.keras` | 早期试上传/探针残留 | 删除 |
| 11 | **高** | `datasets._slice_windows` | `strict=False` 会产出**参差数组** → `np.asarray` 在 numpy≥1.24 必抛 `inhomogeneous shape`；注释却宣称"交由上层补 NaN"（代码里没这一步） | 不再产出短窗口，`strict` 两种取值都跳过越界窗口并在 docstring 说明。实测 IR014（63788 点）两种取值都得到 420 训练窗 / 0 测试窗、窗口长度全为 784 |
| 12 | 中 | `training._train_1dcnn` | 1DCNN 输出层在原脚本里写死 `Dense(10)`，类别数 >10 时训练到一半抛 `label value outside [0,10)`，报错看不出根因 | 训练前校验 `num_classes ≤ 10`，超出给可读错误并建议改用 `cwt_cnn` |
| 13 | 中 | `training._train_1dcnn` 指标 | 某类"测试窗口为 0"（IR014）时 `accuracy` 只在剩余类上算 → **静默虚高** | metrics 新增 `untested_labels` + 中文 `note`，把未参与计算的类别显式列出来 |
| 14 | 高 | `api.py` 全部响应 | 响应里带本机真实目录（`D:\22project\...`、`C:\Users\...`），前端/日志/错误信息全暴露 | 响应出口统一脱敏（JSON + `text/*`）：工作区前缀去掉、用户目录与其它盘符替换成占位符；图片/SSE 放行 |
| 15 | 高 | `inference._predict_adtk` + 上传/训练两侧 | 防护不对称：上传接口刻意**不**反序列化 `.pkl`，推理侧却 `pickle.load` → 上传一个 pkl 就能在服务端执行代码 | 训练产物写 `trusted: True`、上传产物**强制** `trusted: False`（用户自己提供的 meta 也改掉）；推理侧对不可信产物拒解，可用 `MODEL_ALLOW_UNTRUSTED_PICKLE=1` 放开。老产物缺该字段时按可信处理（兼容） |
| 16 | 高 | `api.Train` / `api.Predict` | 落库失败被 `db: {written: false}` 吞掉，HTTP 仍 200，调用方无法用状态码/字段判断 | 落库失败时在响应里加显式 `warning`（训练与推理两处），前端可见 |
| 17 | 高 | `db.insert_inference_task` + `insert_inference_results` | 两次独立 commit → 结果写失败会留下 `Status=成功、Progress=100` 的空任务 | 新增 `insert_task_with_results()`：一个事务写任务 + N 行结果；原两个方法改为复用内部 `_insert_*_cur` 帮助函数（不重复 SQL）。实测任务 #32 明细 3 行、1DCNN 明细 2 行 |
| 18 | 高 | `api.Predict` + `inference` 失败路径 | 只有成功路径写 `ModelInvocations`（还写在结果插入之后）→ 校验被拒、模型缺产物这些**更常见的失败零痕迹**，排查时只剩 Flask 的 500 页 | 新增 `_log_failed_inference()`：在 `predict()` 调用处 catch → 写 `is_success=0` 记录（400/409/500 按异常类型）→ 原样抛出；**只在已有训练记录时写**（拿得到 ModelID 才满足外键，不会为打错的模型名凭空建 `Models` 行）。实测：2 次失败调用各留 1 条 `IsSuccess=0`，Models 仍是 3 行 |
| 19 | 高 | `tabular._ttl_cache` + `api.DatasetUpload` | 数据集体检有 **120 秒 TTL 缓存且没有失效钩子** → 上传成功后响应里的 `listing`、以及随后刷新的列表页**都看不到刚上传的文件**，用户以为上传失败会重复传（同名又被静默覆盖） | 给缓存加 `cache_clear`，上传写完文件后先清缓存再体检。实测：先访问 `/datasets` 把缓存填满 → 上传 → 响应 `listing` 立刻包含新文件 |
| 20 | 中 | `api.DatasetUpload` | 同名文件**静默覆盖**（一个文件 = 一个类别，覆盖等于换掉那个类别的全部数据）；0 字节文件会被当成"空类别"存下来 | 覆盖时回报 `overwritten` + 中文 `hint`；0 字节文件拒收并给出原因 |
| 21 | 低 | `api.DatasetList` | `config.upload_dir.iterdir()` 无存在性保护，目录被删后 `/datasets` 直接 500 | 加 `is_dir()` 判断，当成"暂无上传数据集" |
| 22 | 中 | `training._train_adtk` 阈值标定 | 在**拟合 PCA 的同一批窗口**上取分位数当阈值 → `baseline_false_positive_rate` 按构造必然 ≈ (1−quantile)，是自证不是验证 | 前 70% 窗口拟合、后 30% 留出窗口定阈值并算误报率（窗口太少自动退回全量）；产物记录 `fit_windows`/`calibration_windows`。重训实测：阈值 `0.002091`、留出集误报率 `0.0000%`、正常文件 0/3 异常、故障文件 3/3 异常 |
| 23 | 中 | `training._train_adtk` | adtk **完全忽略 `dataset_dir`**（硬用 CWRU-0HP），前端选了别的数据集也静默按 CWRU 训练 | 优先取请求里的 `dataset_dir`；找不到基线明确报错（不再静默换 `cpu.csv`） |
| 24 | 中 | `api.Train` | `rate` 无校验：`[0,0,0]`→除零、字符串→TypeError、和>1→训练集为空，全部表现为 **500** | 校验"长度 3、非负、和为 1、`rate[1]+rate[2]>0`"，非法一律 400。实测 4 种非法输入全部 400 |
| 25 | **高** | `datasets.load_windows` | 数据集目录里只要多一个**未登记 `.mat`**，`sorted(table, key=class_id)` 就会拿 `None` 与 `int` 比较 → `TypeError` → 训练 500（审计原本只报了"多个未登记文件共用一个 id"，实测比那更严重：直接崩） | 未登记文件各分配一个递增 id；排序改用"生效顺序"key；`num_classes` 改为 `len(table)`。实测：真实目录 0..9 不变；混入 3 个未登记文件后 id = 9/10/11/12 各自独立，`num_classes` 与 `labels` 长度一致（都是 4），未登记文件在 `stats.unregistered_files` 里显式回报 |

### 7.2 未修（按优先级，已定位到行）

> 编号沿用审计原始编号；**#11–#13 已修**，见上表。

| # | 级别 | 位置 | 问题 | 建议 |
|---|---|---|---|---|
| 15 | 高 | `inference.py:355-359` + `registry.py` | 模型名净化已加，但**推理侧仍会 `pickle.load` 未受信产物**（上传侧刻意不解 pickle），防护不对称 | 给上传产物打"未受信"标记，推理时改用 `weights_only=True` 或限制框架 |
| 15 | 高 | `api.py`（`Predict.post` 末尾） | 写库失败被 `_write_db` 吞成 `{"written": false}`，接口**仍返回 200**（原 `console.html` 控制台已删除，这个误导性提示不再存在，但接口本身的行为仍未修） | 落库失败改为 207/500，或在响应里强制前端展示 `db.written=false` |
| 16 | 高 | `db.py:345` 与 `db.py:369-381` | 任务行与结果行**两次独立提交**，结果写失败会留下 `Status=成功、Progress=100` 的**孤儿任务** | 同事务提交，或先写 `Status=运行中` 再 update |
| 17 | 高 | `inference.py:332`（唯一一处 `insert_invocation`） | `ModelInvocations` 只在成功路径写，且写在结果插入之后 → 失败/校验拒绝**零审计**（注释却承诺"每次调用留一条"） | 在 `except` 路径补 `is_success=0` 的调用记录 |
| 18 | 中 | `training.py:444`（`contextlib.redirect_stdout`） | `redirect_stdout` 是**进程级全局**，并发两个 `/train` 会互相串日志、甚至写已关闭的文件 | 改为给 trainer 传 logger，或加"训练互斥锁" |
| 19 | 中 | `training.py:339-349`（adtk） | adtk **完全忽略 `dataset_dir`**（硬用 CWRU 基线）；`number` 被无视（只切 311 窗）；`max_points` 截断信号不回传 | 支持 `dataset_dir`；把实际窗数/截断量写进 `dataset_stats` |
| 20 | 中 | `training.py:383-389` | adtk 阈值在**拟合 PCA 的同一批窗口**上取分位 → `baseline_false_positive_rate` 按构造必然 ≈0.005，属于自证而非验证 | 留出独立验证窗口集再报误报率 |
| 21 | 中 | `tabular.py:205-227` + `api.DatasetUpload` | 表格体检 120s TTL 缓存无失效钩子 → **上传后 2 分钟内列表看不到新文件**（用户会重复上传） | 上传成功后调用 `describe_directory.cache_clear()` |
| 22 | 中 | `datasets.py:49,243` | `_DESCRIBE_CACHE` **只读不写**，注释宣称的 120s 缓存从未生效（每次体检都重跑 `whosmat`） | 补写缓存（返回深拷贝，避免多线程共享 dict） |
| 23 | 中 | `inference._apply_scaler` | `scaler_file` 缺失/文件不在时**静默跳过标准化**，分类结果会完全错 | 至少在响应里加 `warnings` |
| 24 | 中 | `api.py:390`（已改为 `relative_to`）+ `inference._guard_path` + `_resolve_workspace_path` | 三处"工作区内"校验写法各不相同，历史上口径不一致 | 抽成一个 `safe_path()` 工具函数 |
| 25 | 中 | `datasets.py:158` | 未登记 `.mat` 全部拿到 `class_id=len(table)`（循环内是常量）→ 多文件同类、`labels` 与 `num_classes` 长度不一致 | 未登记文件应报错或按文件名单独编号 |
| 26 | 中 | `training.py:113-118` | 目录里同时有 `.mat` 和表格时**表格优先**，`.mat` 被静默忽略 | 显式要求 `dataset_type`，或两者都在时报错 |
| 27 | 中 | `training.py:146,227` + `datasets.py:148,208` | `rate` 无任何校验：和为 0 → 除零；和 >1 → 训练集为空；字符串 → `TypeError`；`rate[0]` **从不参与运算** | 校验 `len(rate)==3`、和为 1、每项 >0，并让 `rate[0]` 真正生效 |
| 28 | 中 | `inference.py:65-77` vs `datasets._slice_windows` | 推理按**非重叠** `input_len` 切窗，训练用 `stride`（默认 150）重叠切窗 → 窗口编号语义对不上 | 在 meta 里记录 `stride`，推理可选按同样 stride 切 |
| 29 | 低 | `registry.py:133-136`（`_prune_except`）、`registry.latest_meta`、`config.Config.defaults`（全仓无引用） | 死代码；`defaults` 无引用导致默认超参在三处副本里漂移 | 删除死代码；让 `training.py` 真正读 `config.defaults` |
| 30 | 低 | `api.py:326`、`aliases`/命名 | 上传数据集名默认取"第一个文件名"；`describe_dataset` vs `describe_directory`、两套 `load_windows`、`1dcnn` vs `1DCNN` 命名不统一 | 统一命名与默认值来源 |

---

## 8. 可读性改进

**已做**

- 新增根目录 [`README.md`](README.md)：目录结构、模块地图、怎么跑、常用命令、已知限制。
- 新增本文档：把"数据集格式 / 四个模块流程 / Bug 清单"从代码里抽出来。
- `testRestfulProject/main.py` 清掉 flask_restful 官方示例残留（`TODOS`、`/todos`、`Todo`、`TodoList`、`PcaAD` 演示导入），现在只剩"注册两层路由 + 启动"。
- 后端每个模块都有模块级 docstring；关键函数注释说明"为什么这么做"（尤其是踩过的坑）。

**建议继续做**

- `training.py`（535 行）与 `api.py`（850+ 行）按职责再拆：`trainers/`、`resources/`。
- `api.py` 里的路径解析、body 解析、错误包装抽成 `api_helpers.py`。
- 前后端字段命名统一：后端 `PredictedLabel` ↔ 前端 `predicted_label`（JSON 已统一小驼峰，但库表是帕斯卡），建议在 `db.py` 里集中做一次映射并写进文档。
- 给 `_selftest_upload.py` 补一个"训练→推理→落库"的端到端自测，替代手工 curl。
